Entities are special bean classes that interface with the database in the DAO. They are abstractions of
the database tables, meaning that they are a direct line to the database, so DON'T use them outside of the
DAO.